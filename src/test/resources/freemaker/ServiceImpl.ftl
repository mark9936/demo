package com.demo.repo.freemaker.target.serviceimpl;

import com.demo.repo.freemaker.target.service.${tableBean.entityName}Service;
import org.springframework.stereotype.Service;

/**
* @Function: ${tableBean.entityName}ServiceImpl.java
* @Description: ${tableBean.tableName} ServiceImpl
* @author: ${tableBean.userName}
* @date: ${.now ? string["yyyy/MM/dd"]}
* @MaintenancePersonnel: ${tableBean.userName}
*/
@Service
public class ${tableBean.entityName}ServiceImpl implements ${tableBean.entityName}Service {

}
